# Runtime Download Example

Add `RuntimeDownloadExample` to a GameObject, set a public GitHub repository URL, and enable **Download On Start** or use the component context menu.

Do not embed private repository tokens in a player build. For authenticated downloads, obtain short-lived credentials from a service you control.
